import { api } from '$lib/api';
import type { MetricsResponse } from '$lib/api/v1/types/metrics.js';

export const metricsApi = {
	get: (hours = 24) => api.get<MetricsResponse>(`/api/v1/system/metrics?hours=${hours}`),
};

































































































